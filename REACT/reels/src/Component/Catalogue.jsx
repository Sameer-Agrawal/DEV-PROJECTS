import React from 'react'

function Catalogue( { perception } ) {
    return (
        <React.Fragment>
            {/* { console.log(perception) } */}
        </React.Fragment>
    )
}

export default Catalogue;